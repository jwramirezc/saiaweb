// Capturamos el elemento select
let selectElement = document.getElementById('opciones');
let control = [];

// Añadimos un evento para capturar el valor seleccionado cada vez que cambie
selectElement.addEventListener('change', function () {
  let valorSeleccionado = selectElement.value;
  console.log('Valor seleccionado:', valorSeleccionado);
});

if (condition) {
}
